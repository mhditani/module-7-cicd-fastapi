name: Module 7 CI/CD Workflow

on: [push]

jobs:
  web:
    runs-on: ubuntu-latest

    services:
      db:
        image: postgres:latest
        env:
          POSTGRES_USER: pro_role
          POSTGRES_PASSWORD: mypassword
          POSTGRES_DB: myappdb
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U pro_role"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://pro_role:mypassword@localhost:5432/myappdb

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest